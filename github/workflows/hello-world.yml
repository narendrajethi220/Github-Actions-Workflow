name: Hello World
run-name: helloWorld actions is running
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      -run: echo "🎉 The job is automatically triggered by a ${{github.event_name}} event."